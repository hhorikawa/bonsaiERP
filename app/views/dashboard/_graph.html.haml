%h4.n.ib.muted.graph
  Graph of Daily <span class="text-success">Income</span> vs <span class="text-error">Expense</span>

%div(data-controller="dashboard")
  %div
    params

  %div(id="chart")
    replace here



:javascript
  $(function() {
    var rep = new App.GraphReport('#report1', #{data.incomes_dayli.to_json}, #{data.expenses_dayli.to_json})
  })
