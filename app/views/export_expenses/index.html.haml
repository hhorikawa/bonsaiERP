%h1 Export Expenses

.row-fluid.form-inline.well.well-small
  .span12
    = form_tag export_expenses_path(format: 'xls') do
      = render 'layouts/date_range', dr: @date_range
      = submit_tag 'Export a Excel', class: 'btn'
