- content_for :links do
  %ul.breadcrumbs.span6
    %li
      = ""

.row-fluid
  .span6
    %h1.ib= "Sales Orders"
    = search_tag
  .span6.buttons.r

    %batch-operations(url="#{ income_batch_payments_path }" model-type="Income" model-name="Ingreso")

      %li
        = link_to export_incomes_path do
          %i.icon-download-alt
          = t("general.export")

    %tags(model='Income' tag-ids='#{ tag_ids }' url='#{ }')

    = link_to "New Sales Order...", new_sales_order_path, class: 'btn btn-secondary'


= render partial: "sales_orders/search"


- paginate_html = paginate @orders

= paginate_html

%ul#incomes-list.unstyled.listing.has-row-check
  %li.row-fluid
    .span4.offset7
      .span6.r.b.muted
        Total
      .span6.r.b.muted
        Saldo

  = render partial: 'sales_orders/income', collection: @orders, as: :income

= paginate_html

-#= render 'incomes/index_tour'
