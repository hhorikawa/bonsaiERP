= bootstrap_form_for(@unit) do |f|
  .row-fluid
    .span6
      = f.text_field :name, :label => 'Name:', :input_html => {:size => 30}
    .span6
      = f.text_field :symbol, :label => 'Symbol:', :input_html => {:size => 12}

  .form-action
    = f.submit 'Save', class: 'btn btn-success b'
