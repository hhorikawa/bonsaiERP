
-# @param order order object

%tr
  %td
    %input.row-check(type='checkbox' id="#{order.id}")
    %h4.ib
      = link_to order.id, order
  %td= link_to order.contact, partner_path(order.contact.id)

  %td= order.state
  %td= order.date
-# if expense.tax

-#      = expense.inventory_tag
-#      %tagsfor(tag_ids="#{expense.tag_ids}" id="#{expense.id}")


  %td
    = order.updater
    = order.creator
    = order.approver
    = order.nuller
  %td
    = order.delivery_date

  %td
    = order.total
    = order.currency

  %td
    = order.balance
  %td
    = link_to "Edit...", edit_purchase_order_path(order.id), class: 'dark' if !order.nulled?
