- content_for :links do
  %ul.breadcrumbs.span6
    %li
      &nbsp;


%div.row
  %div.col
    %h1.ib Purchase Orders

  %div.btn-list
    = link_to "Export", export_expenses_path, class: 'btn btn-secondary' 

    = link_to "New Purchase Order...", new_purchase_order_path, class: 'btn btn-secondary'



= render partial: 'movements/search'


- paginate_html = paginate @orders

=paginate_html

%ul#expenses-list.unstyled.listing.has-row-check
  - @orders.each do |order|
    = render 'purchase_orders/expense', order: order

=paginate_html

-#= render 'expenses/index_tour'
