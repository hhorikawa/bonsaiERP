// JavaScript version of dynamic.coffee
// Part of the CoffeeScript to JavaScript migration

// Controller to load dynamically HTML
myApp.controller('DynamicController', ['$scope', function($scope) {
  // htmlContent
  $scope.htmlContent = '';
}]);
